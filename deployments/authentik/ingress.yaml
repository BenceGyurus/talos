apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: authentik-ingress
  namespace: default
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`auth.gyurus.hu`)
      kind: Rule
      services:
        - name: authentik-server
          port: 80
