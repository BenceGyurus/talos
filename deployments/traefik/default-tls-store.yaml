apiVersion: traefik.io/v1alpha1
kind: TLSStore
metadata:
  name: default
  namespace: traefik   # FONTOS: Ugyanaz a namespace, ahol a Secret és a Traefik van!
spec:
  defaultCertificate:
    secretName: gyurus-tls
