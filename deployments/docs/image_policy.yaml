apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: docs-image
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: docs-image
  policy:
    semver:
      range: 1.x.x